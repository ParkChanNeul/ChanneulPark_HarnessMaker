# Workspace

This folder stores audits, intermediate handoffs, validation notes, partial rerun state, and run artifacts.

Tracked workspace files may contain synthetic or public-safe notes only. Runtime runs under `_workspace/runs/` are ignored because real executions may include private learner context.
